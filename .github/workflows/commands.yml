on:
  workflow_dispatch:

jobs:
  example:
    runs-on: ubuntu-latest
    steps:
    - name: Set API key
      run: echo "API_key=12345" >> $GITHUB_ENV
    - name: Use api key
      run: echo "useing api key $API_key"
